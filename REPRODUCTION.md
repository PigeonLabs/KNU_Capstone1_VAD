# 논문–공개 코드 차이와 구현 결정

기준: `ipad2404.15033v1.pdf` §4–5, Table 2–4. 사용자 지시에 따라 논문을 우선하며 미기재 사항은 가정으로 표시합니다.
이는 원 저자의 비공개 구현과의 동일성을 보장하지 않습니다.

| 항목 | 확인 내용 | 현재 구현 |
|---|---|---|
| 메모리 배치 | 공개 코드에서 `i` 미정의, 배치와 토큰이 합쳐짐 | `[B,T,M]` 주소를 유지하고 배치별 위상 적용 |
| 위상 매핑 | 200 클래스인데 분모 126 사용 | `floor(phase × M / 200)`, 범위 보장 |
| 강조 슬롯 | 수식 (4)는 한 슬롯, 코드는 ±7 슬롯 | 기본 한 슬롯; `--radius 7`로 별도 비교 가능 |
| 강조 점수 | 공개 코드는 max raw logit, 논문 P_score의 정규화 미명시 | softmax 확률의 최댓값 사용 **가정** |
| 메모리 softmax | 수식 (5)는 T축, 코드는 M축 | 기본 token 축; `--axis memory`로 비교 가능 |
| sparsity | 코드 shrink threshold .0025, 논문 해당 수식 미기재 | 기본 없음; `--shrink .0025`로 별도 비교 |
| 손실 | 논문 전체 loss/가중치 미명시 | 코드의 clip MSE + .02 CE + .0002 entropy; entropy는 선택한 softmax 축에서 계산 |
| 위상 supervision | 공개 loader는 clip 시작 위치 | 정상 영상 clip start 기준 200 구간. 평가 재구성 대상은 local index 8 |
| 주기 검사 | 논문 window 5, 공개 평가 코드에 점수 반영 없음 | 영상 내 centered window 5; 양끝 2개 clip은 공통 평가에서 제외 |
| 주기 기준 | 길이가 200이 아닐 때 기준 수열 상세 미기재 | 정상 학습 영상 길이 중앙값 L로 증가량 200/L, 원형 위상 거리 **가정** |
| 점수 결합 | 최종 결합식 미명시 | 두 scene-normalized 점수의 0.5/0.5 평균 **가정** |
| 재구성 점수 | 공식 PSNR은 정상 점수 | `negative_psnr = 10 log10(MSE)`, 큰 값=이상으로 통일 |
| backbone | 소스 기본 patch (4,4,4), window (2,7,7), pretrained 미사용 | 소스 생성자 유지, 출력 `[B,768,4,8,8]` |
| 위상 head | 소스 FC `49152→4096→2048→200` | 그대로 유지. 전체 263.48M으로 논문 표 35.9M과 불일치 |
| 입력 색 | 공식 loader는 OpenCV BGR [-1,1] | 원 모델은 그대로 유지; DINOv2에서만 RGB/ImageNet 정규화 |
| 학습 수 | 논문 50, 공개 CLI 기본 200 epochs | 50 epochs |
| 초기화/전이 | Table 3은 실데이터-only와 synthetic 전이를 분리 | real-only; S01–S12 및 LoRA 제외 |
| 테스트 정규화 | 논문은 장면 전체, 짧은 영상별 아님 | 장면 전체 min-max. 상수 점수는 0으로 처리 |
| 시간 정보 | 테스트 주기 길이로 위상을 알려주면 누수 | 모델 입력에서 테스트 phase/labels 제외; 길이는 평가 정렬에만 사용 |

`--axis memory --radius 7 --shrink .0025`는 공개 코드와 유사한 메모리 옵션입니다.
잘못된 배치 처리·126 상수와 raw logit 강화는 그대로 복제하지 않으므로 이를 '원 코드 완전 동일'로 부르지 않습니다.

## 프레임 정렬 및 데이터 제한

- 원본 jpg는 0부터 시작합니다. clip start `s`의 입력은 `s..s+15`, 재구성 대상은 `s+8`입니다.
- 길이 N 영상의 원 재구성 유효 프레임은 `8..N-8`입니다. 주기 window까지 공통 적용하면 `10..N-10`입니다.
- R02: 영상 12는 806/805, 13은 609/608, 14는 497/498 (이미지/라벨)입니다.
- 불일치 영상은 strict 평가에서 제외합니다. 보조 분석은 원본 인덱스 기준 공통 범위와 label index ±1을 사용하며,
  이 보조 분석을 근거로 라벨을 자동 보정하거나 최선의 AUROC를 선택하지 않습니다.
- binary label은 1=이상입니다. 단일 클래스 전체 집합이면 AUROC/AUPRC를 null로 반환합니다.
- 외형/시간 이상 분류 정답이나 pixel mask는 제공된 실제 데이터에서 확인되지 않았으므로 유형/픽셀 단위 성능은 보고하지 않습니다.

## DINOv2 비교 설정

두 비교에서 frozen ViT-B/14, 252×252, RGB/ImageNet normalization, L2-normalized 특징을 씁니다.
A는 동일한 원 모델 checkpoint에서 입력/복원 영상의 CLS·6층 patch·12층 patch·6/12층 평균을 비교합니다.
각 patch 오차의 평균으로 frame score를 구하며, 모든 점수와 주기 결합을 각각 기록합니다. DINOv2 기반 재학습은 하지 않습니다.

B는 16개의 CLS 순서를 보존한 `12288→512→200` 위상 MLP를 학습합니다. phase 200개를 메모리 20개 구간으로 매핑합니다.
정상 clip의 중앙 프레임 patch는 해당 clip 시작 위치의 위상에 배정하여 원 모델의 supervision 기준과 맞춥니다.
위상별 최대 512개의 정상 clip을 seed 기반으로 뽑아 각 공간 위치에서 spherical k-means 20회로 최대 10개 prototype을 만듭니다.
추론 단계에는 입력 clip에서 예측한 위상만 사용합니다. 비어 있는 구간은 가장 가까운 점유 원형 위상 구간으로 처리합니다.

무조건부 메모리는 **동일하게 선택된 정상 특징**으로 동일한 전체 prototype 수를 학습합니다.
각 위치마다 cosine NN과 온도 .1의 softmax 가중 prototype을 구한 뒤, 정규화한 projection과 입력의 cosine 잔차를 비교합니다.
메모리 두 종류의 저장 바이트 및 실제 prototype 수를 결과에 남깁니다. 특징과 prototype의 저장 dtype은 FP16이며 연산은 FP32입니다.
캐시 특징 추출 시간+매칭 시간은 offline 회계값이며 streaming 실측 지연과 구분합니다.

## 성공과 후속 반복

1. 데이터/CUDA 검사, 작은 정상 배치 손실 감소, gradient/체크포인트 동등성.
2. seed 0 장면별 50 epochs와 프레임 평가 완료. 논문 수치에 대한 절대 차이 기록.
3. 같은 프레임에서 A/B, 주기 점수 제거, 메모리 제거 모델 비교.
4. 논문 성능과의 격차 원인을 검토하고 설정을 고정한 다음 seed 1·2 반복.

seed 반복 실행은 CLI가 지원하지만 미완료 seed 0의 문제를 확인하지 않고 자동으로 반복하지 않습니다.
학습은 모든 정상 데이터로 사전 고정한 논문 설정을 사용하며 최종 epoch를 평가합니다. 테스트 AUROC 최고 checkpoint를 선택하지 않습니다.
